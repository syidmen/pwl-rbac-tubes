# Domain Inventaris Barang

Studi kasus project ini adalah Inventaris Barang Sederhana.

Model utama:

```text
Item
```

Endpoint yang direncanakan:

```text
GET /items
POST /items
GET /items/:id
PATCH /items/:id
DELETE /items/:id
```
